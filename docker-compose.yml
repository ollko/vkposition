

services:
  web:
    build:
      context: ./fastapi-app
      dockerfile: Dockerfile
    command: uvicorn src.web.main:app --host 0.0.0.0
    volumes:
      - ./fastapi-app:/src/fastapi-app
    ports:
      - 8008:8000